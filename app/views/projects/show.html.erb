<h1><%= @project.name %></h1>
<p><%= @project.description %></p>
<h2>Aguardando aprovação (<%= @pending_posts.size %>)</h2>
<ul>
  <% @pending_posts.each do |post| %>
    <li><%= link_to post.caption&.truncate(60) || "Post ##{post.id}", [@project, post] %></li>
  <% end %>
</ul>
